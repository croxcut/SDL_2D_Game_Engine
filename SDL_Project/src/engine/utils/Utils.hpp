#ifndef UTILS_HPP
#define UTILS_HPP


#include <string>

/*
	This Header File Handles most of the Utility
	( might delete this one and find a much more 
	efficient way to do this, remember this is 
	just for the core Engine itself things might
	change during the development :D* )
*/

const int SCREEN_HEIGHT = 650;
const int SCREEN_WIDTH = SCREEN_HEIGHT * 16 / 10;
const std::string TITLE = "Just A Window";

#endif